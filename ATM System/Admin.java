public class Admin extends Account {

    public Admin(String accountId, String Pin) {  // Constructor to initialize admin ID and PIN
        super(accountId, Pin);  // Pass admin Id and Pin to super class
    }
}