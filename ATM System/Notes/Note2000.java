package Notes;

import Note.Notes;

public class Note2000 extends Notes {
    public Note2000(int noteCount) { // Constructor to create a Note2000 object
        super(noteCount, 2000); // Calling the parent class constructor with noteCount and noteValue of 2000
    }
}
