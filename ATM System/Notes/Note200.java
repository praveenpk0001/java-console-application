package Notes;

import Note.Notes;

public class Note200 extends Notes {
    public Note200(int noteCount) { // Constructor to create a Note200 object
        super(noteCount, 200); // Calling the parent class constructor with noteCount and noteValue of 200
    }
}
