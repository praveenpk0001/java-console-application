package Notes;

import Note.Notes;

public class Note500 extends Notes {
    public Note500(int noteCount) { // Constructor to create a Note500 object
        super(noteCount, 500); // Calling the parent class constructor with noteCount and noteValue of 500
    }
}
