package Notes;

import Note.Notes;

public class Note100 extends Notes {
    public Note100(int noteCount) { // Constructor to create a Note100 object
        super(noteCount, 100); // Calling the parent class constructor with noteCount and noteValue of 100
    }
}
